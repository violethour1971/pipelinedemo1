name: pipelinedemo1

on:
  pull_request:
    branches: [ master ]
    types: [ opened ]

  # can run manually for testing
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: trigger Jenkins to compile
      run: nc --help


