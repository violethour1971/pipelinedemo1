pipeline {
    agent any 
    stages {
        stage('Stage 1') {
            steps {
                echo 'Initialization'
                sh 'date >> ./pipelinedemo1_shell_output.txt'
                echo 'Finished'
            }
        }
    }
}
